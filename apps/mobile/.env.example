# Local development environment variables
# Expo requires all client-side env vars to start with EXPO_PUBLIC_

# API base URL - localhost:8080 for local backend
# NOTE: Use your machine's local IP (e.g. 192.168.x.x) when testing on a physical device
EXPO_PUBLIC_API_URL=http://YOUR_LOCAL_IP:8080

