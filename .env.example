# ─── PostgreSQL Database ──────────────────────────────────────────────
DB_PASSWORD=1234

# ─── Security (JWT) ───────────────────────────────────────────────────
# Use a strong, random 256-bit base64-encoded string for production.
# Example generator: `openssl rand -base64 32`
JWT_SECRET=CHANGE_ME_USE_A_STRONG_256_BIT_SECRET_KEY_HERE
